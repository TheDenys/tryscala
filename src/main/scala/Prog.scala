import EulerCommon._

object Prog {
  def main(args: Array[String]): Unit = {
    println("hiho!")

    //    for (i <- 1 to 20) {
    //      val p = isPrime(i)
    //      val mark = if (p) "p" else ""
    //      println(s"${i} ${mark}")
    //    }
    //
    //    var l: Long = 1500000000000L
    //
    //    while (true) {
    //      val mark = isPrime(l)
    //
    //      if (mark) {
    //        println(s"${l}")
    //      }
    //
    //      l = l + 1
    //    }

    Equator.demo()
  }
}
